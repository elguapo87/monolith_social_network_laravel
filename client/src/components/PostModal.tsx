import { FeedsData } from "@/context/UserContext";

type PostCardProps = {
    setShowPost: React.Dispatch<React.SetStateAction<boolean>>;
    post: FeedsData;
};

const PostModal = ({ setShowPost, post } : PostCardProps) => {
  return (
    <div>
      PostModallllllllllllllllllll
    </div>
  )
}

export default PostModal
